
// to know app package & app activity


1|G2299:/ $ dumpsys window windows | grep -E 'mCurrentFocus'

// Enter this command in cmd

  mCurrentFocus=Window{f6cf512 u0 com.amazon.mShop.android.shopping/com.amazon.mShop.sso.SigninPromptActivity}